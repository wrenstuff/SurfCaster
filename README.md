# SurfCaster

How to install requiremnts.
In the power shell (terminal) type or paste the following. 

This will create a folder to install the programs dependencies inside of.
python -m venv venv

Then use the code below  to get inside of the venv file
.\venv\Scripts\Activate.ps1

Use the below code to then install the project dependencies inside the secure environment. (note this will take a while as some libraries are large)
pip install -r requirements.txt

running the application will require you to run the code below
python surfcaster/app.py     


If any issues should arrise please contact us directly at surfcaster.app@gmail.com